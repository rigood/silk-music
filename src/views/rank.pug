extends base
include mixins/member

block content
    .left
        h2 🏆 스트리밍 랭킹
        .desc
            div 🎀 가수 &lt;진욱&gt; 님의 노래 1회 재생 시 포인트 1점 획득
            div 🎀 동점일 경우, 가입일이 최근일수록 높은 순위 부여
        ul
            each member, index in members
                +member(member, index, page, perPage)
            else
                .msg="대상자가 없습니다."
        if(members.length )
        .pagination
            - for (var i=0; i < totalPage; i++)
                a(href=`/rank?page=${i+1}` class=`${page===i+1 && "active"}`)=i+1