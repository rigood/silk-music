extends ../base

block content
    .center
        h2 🌟 새 플레이리스트 🌟

        if errorMsg
            .errorMsg=errorMsg

        form(method="POST")
            .label 플레이리스트 이름
            input(name="name" id="name" placeholder="Playlist name" type="text" minLength="1" maxLength="20" required)

            .label (선택) 플레이리스트 커버 이미지 주소
            input(name="coverUrl" id="coverPreviewInput" placeholder="(Optional) Playlist cover image url" type="search").noMarginBottom

            button(type="button")#coverPreviewBtn.noMarginBottom 미리보기 (Preview)
            #coverPreview

            button(type="submit") 추가 (Add)

    script(src="/static/js/coverPreview.js") 